% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_duckdb.R
\name{snowball_duckdb_config}
\alias{snowball_duckdb_config}
\title{Configuration for the DuckDB connections used by a snowball}
\usage{
snowball_duckdb_config(
  memory_limit = "auto",
  temp_directory = "auto",
  threads = "auto",
  preserve_insertion_order = FALSE,
  partitioned_write_max_open_files = NULL,
  max_temp_directory_size = NULL,
  concurrency = 1L
)
}
\arguments{
\item{memory_limit}{DuckDB \code{memory_limit}. \code{"auto"} (the default) derives a
budget from physical RAM and \code{concurrency}; \code{NULL} leaves DuckDB's own
default of ~80\% of RAM; anything else is passed through (e.g. \code{"8GB"}).}

\item{temp_directory}{Root for DuckDB's spill files. \code{"auto"} (the default)
uses a private directory under \code{\link[=tempdir]{tempdir()}}. A path you supply is treated
as a \strong{root}: a \verb{<pid>-<output hash>/<stage>} leaf is always appended, so
setting one path for a whole worker pool stays safe. \code{NULL} leaves
DuckDB's \code{.tmp}, which concurrent callers share -- not recommended.}

\item{threads}{\verb{SET threads}. \code{"auto"} divides the available cores by
\code{concurrency}; \code{NULL} leaves DuckDB's default.}

\item{preserve_insertion_order}{Defaults to \code{FALSE}. Row order on disk is
not part of this package's contract -- \code{\link[=read_snowball]{read_snowball()}} sorts nodes by
\verb{(desc(oa_input), id)} and edges by \verb{(from, to)} -- and preserving it
makes the partitioned writes buffer.}

\item{partitioned_write_max_open_files}{\code{NULL} (default) leaves DuckDB's
100. Do not raise it here: \verb{nodes/} and \verb{edges/} have three partitions
each, so 100 is already ~33x what is needed, and each open file carries a
write buffer. (openalexSnapshot raises it to 512, but that write fans out
over hundreds of partitions.)}

\item{max_temp_directory_size}{\code{NULL} (default) leaves DuckDB's 90\% of free
disk.}

\item{concurrency}{How many \code{pro_snowball()} calls you intend to run at the
same time. Used to divide the memory and thread budgets. There is no
portable way to detect sibling R processes, so this is declared rather
than discovered -- set it once for a worker pool:
\code{options(openalexSnowball.duckdb_config = list(concurrency = 4))}.}
}
\value{
A list of class \code{openalexSnowball_duckdb_config}.
}
\description{
\code{pro_snowball()} opens DuckDB twice -- once to assemble \verb{nodes/}, once to
extract \verb{edges/}. This builds the settings applied to both.
}
\details{
Settings resolve \strong{per field}, not per object: an explicit argument field
beats the same field of \code{getOption("openalexSnowball.duckdb_config")},
which beats the package default. So setting one field in the option does
not silently discard the safe defaults for the others, and passing
\code{duckdb_config = list(threads = 2)} at a call site does not discard a
session-wide memory budget.

Note that \code{memory_limit} is a fairness and blast-radius control rather than
a correctness one: lowering it does not make a query fit, it makes a
failure arrive sooner and stops one run from starving the others.
}
\examples{
snowball_duckdb_config(memory_limit = "8GB", concurrency = 4)

}
